import {
    CalendarProps,
    MultipleCalendarProps,
    RangeCalendarProps,
} from '@yandex-lego/components/next/Calendar/desktop/bundle';

export const Calendar = (_props: CalendarProps) => null;

export const MultipleCalendar = (_props: MultipleCalendarProps) => null;

export const RangeCalendar = (_props: RangeCalendarProps) => null;
