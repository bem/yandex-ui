import { Pseudo } from '@yandex-lego/components/Link/__examples__/pseudo.tsx'
import pseudoSource from '!!raw-loader!@yandex-lego/components/Link/__examples__/pseudo.tsx'
import { View } from '@yandex-lego/components/Link/__examples__/view.tsx'
import viewSource from '!!raw-loader!@yandex-lego/components/Link/__examples__/view.tsx'
import { Theme } from '@yandex-lego/components/Link/__examples__/theme.tsx'
import themeSource from '!!raw-loader!@yandex-lego/components/Link/__examples__/theme.tsx'
import { Disabled } from '@yandex-lego/components/Link/__examples__/disabled.tsx'
import disabledSource from '!!raw-loader!@yandex-lego/components/Link/__examples__/disabled.tsx'

### Ссылка view="default"

Чтобы изменить вид ссылки, установите свойство `view` в значение `dеfault`.

<Example
    component={View}
    source={viewSource}
/>

### Псевдоссылка

Чтобы сделать псевдоссылку, установите свойство `pseudo`.

Клик по такой ссылке не вызывает перехода на страницу, указанную в свойстве `href`, а вызывает событие, которое указано в свойстве `onClick`.

Псевдоссылки используются для того, чтобы открыть/скрыть какую-то информацию на странице, например показать всплывающую подсказку.

Для оформления псевдоссылок можно использовать тему `pseudo`.

<Example
    component={Pseudo}
    source={pseudoSource}
/>

### Стилевое оформление

Чтобы изменить стилевое оформление ссылки, установите свойство `theme` в одно из следующих значений: `"black"`, `"ghost"`, `"normal"`, `"outer"`, `"pseudo"`, `"strong"`.

<Example
    component={Theme}
    source={themeSource}
/>

### Недоступная ссылка

Чтобы сделать ссылку недоступной, установите свойство `disabled`.

<Example
    component={Disabled}
    source={disabledSource}
/>
