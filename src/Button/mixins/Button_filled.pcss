@define-mixin Button_filled
    $viewName,
    $colorBase,
    $fillColorBase,
    $colorDisabled,
    $fillColorDisabled,
    $colorChecked,
    $fillColorChecked {
    /* common */
    @mixin Button_base
        $viewName,
        $colorBase,
        $fillColorBase;
    /* press */
    @mixin Button_pressed
        $viewName;

    /* disable */
    @mixin Button_disabled
        $viewName,
        $colorDisabled,
        $fillColorDisabled;

    /* check */
    @mixin Button_checked
        $viewName,
        $colorChecked,
        $fillColorChecked;
}
