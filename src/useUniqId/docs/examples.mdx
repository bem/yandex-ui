---
id: UseUniqId
---

import { Default as useUniqIdDefault } from '../examples/default.examples.tsx';
import useUniqIdDefaultSource from '!!raw-loader!../examples/default.examples.tsx';

Хук `useUniqId` используется для генерации уникальных `id` с читаемым префиксом.
Можно назначить уникальные `id`, используя имя компонента в качестве префикса, чтобы можно было удобно определять компонент при дебаге.

<Example
    component={useUniqIdDefault}
    source={useUniqIdDefaultSource}
/>