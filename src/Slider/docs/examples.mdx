---
id: slider
---

import { Basic as SliderBasic} from '../Slider.examples/desktop/basic.examples.tsx'
import basicSliderSource from '!!raw-loader!../Slider.examples/desktop/basic.examples.tsx'

import { Filled as SliderFilled } from '../Slider.examples/desktop/filled.examples.tsx'
import filledSliderSource from '!!raw-loader!../Slider.examples/desktop/filled.examples.tsx'

import { Reversed as SliderReversed} from '../Slider.examples/desktop/reversed.examples.tsx'
import reversedSliderSource from '!!raw-loader!../Slider.examples/desktop/reversed.examples.tsx'

import { Stepped as SliderStepped} from '../Slider.examples/desktop/stepped.examples.tsx'
import steppedSliderSource from '!!raw-loader!../Slider.examples/desktop/stepped.examples.tsx'

import { Marked as SliderMarked } from '../Slider.examples/desktop/marked.examples.tsx'
import markedSliderSource from '!!raw-loader!../Slider.examples/desktop/marked.examples.tsx'

import { CustomThumb as SliderCustomThumb } from '../Slider.examples/desktop/customThumb.examples.tsx'
import customThumbSliderSource from '!!raw-loader!../Slider.examples/desktop/customThumb.examples.tsx'

import { CustomLabelTick as SliderLabelTick } from '../Slider.examples/desktop/customLabelTick.examples.tsx'
import labelTickSliderSource from '!!raw-loader!../Slider.examples/desktop/customLabelTick.examples.tsx'

import { Showcase as SliderShowcase } from '../Slider.examples/desktop/showcase.examples.tsx'

### Базовый

В самом простом случае достаточно указать `view` и `value`. Для того, чтобы задать диапазон, необходимо в свойство `value` передать два числа.

<Example
    component={SliderBasic}
    source={basicSliderSource}
/>

### Заполненный

Чтобы показать индикатор выбранного диапазона, установите свойство `filled`.

<Example
    component={SliderFilled}
    source={filledSliderSource}
/>

### Развернутый

Чтобы отобразить слайдер в обратном направлении, установите свойство `reverse`.

<Example
    component={SliderReversed}
    source={filledSliderSource}
/>

### Дискретный

Чтобы задать шаг смещения, установите свойство `step` с необходимым значением.

> ⚠️ При больших значениях min/max рекомендуется так же устанавливать большое значение для шага.

<Example
    component={SliderStepped}
    source={steppedSliderSource}
/>

### Маркированный

Чтобы разметить слайдер или показать значения в определенном шаге, установите следующие свойства:

- `showTicks` — Показывает метки на треке
- `showTickValues` — отображает значения на метках трека (по умолчанию показывает только минимальное и максимальное значения)

> ⚠️ Не рекомендуется использовать данные свойства при очень больших значениях (min/max) и маленьком шаге (step), т.к. они создают лишние DOM-узлы.

<Example
    component={SliderMarked}
    source={markedSliderSource}
/>

### Пользовательский Thumb

При необходимости можно заменить элемент `Thumb` на свою реализацию, для этого установите свойство `renderThumb` со своим компонентом.

<Example
    component={SliderCustomThumb}
    source={customThumbSliderSource}
/>

### Пользовательский LabelTick

При необходимости можно заменить элемент `LabelTick` на свою реализацию, для этого установите свойство `renderTickLabel` со своим компонентом.

<Example
    component={SliderLabelTick}
    source={labelTickSliderSource}
/>

### Showcase

Примеры использования `Slider` также можно посмотреть в [Storybook](https://lego.yandex-team.ru/components/lego/latest/?storybook=%2Fstory%2Fcontrols-slider-desktop--showcase)

<Example
    component={SliderShowcase}
/>
