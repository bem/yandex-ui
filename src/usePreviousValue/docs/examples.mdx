---
id: UsePreviousValue
---

import { Default as usePreviousValueDefault } from '../examples/default.examples';
import { Rerender as usePreviousValueRerender } from '../examples/rerender.examples';
import usePreviousValueDefaultSource from '!!raw-loader!../examples/default.examples';
import usePreviousValueRerenderSource from '!!raw-loader!../examples/rerender.examples';

Хук `UsePreviousValue` сохраняет значение свойства из предыдущего рендера. 

<Example
    component={usePreviousValueDefault}
    source={usePreviousValueDefaultSource}
/>

Важное замечание, что хук сохраняет не предыдущее значение свойства, а значения свойства из **предыдущего рендера**.

<Example
    component={usePreviousValueRerender}
    source={usePreviousValueRerenderSource}
/>