import React from 'react';

import { Icon } from '@yandex-lego/components/Icon/desktop/bundle';

export const Custom = () => (
    <Icon url={require('./custom-icon.svg')} style={{ width: 16, height: 16 }} />
);
