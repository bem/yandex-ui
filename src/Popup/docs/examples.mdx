---
id: Popup
---

import { Target as PopupTarget } from '../__examples__/target.tsx'
import popupTargetSources from '!!raw-loader!../__examples__/target.tsx'
import { View as PopupView } from '../__examples__/view.tsx'
import popupViewSources from '!!raw-loader!../__examples__/view.tsx'
import { Theme as PopupTheme } from '../__examples__/theme.tsx'
import popupThemeSources from '!!raw-loader!../__examples__/theme.tsx'
import { Nonvisual as PopupNonvisual } from '../__examples__/nonvisual.tsx'
import popupNonvisualSources from '!!raw-loader!../__examples__/nonvisual.tsx'
import { Direction as PopupDirection } from '../__examples__/direction.tsx'
import popupDirectionSources from '!!raw-loader!../__examples__/direction.tsx'
import { Boundary as PopupBoundary } from '../__examples__/boundary.tsx'
import popupBoundarySources from '!!raw-loader!../__examples__/direction.tsx'

### Привязывание к блоку

Чтобы попап прикреплялся к нужному элементу, установите свойство `target` в значение `anchor`, а в свойстве `anchor` нужный элемент.

<Example
    height={150}
    component={PopupTarget}
    source={popupTargetSources}
/>

| Свойство                  | Тип                                        | По умолчанию                                                                                                                                                                     | Описание                                                  |
| ------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| anchor?                   | `RefObject<HTMLElement>`                   | —                                                                                                                                                                                | Элемент, относительно которого позиционируется попап      |
| directions?               | `Direction[]`                              | `['bottom-left', 'bottom-center', 'bottom-right', 'top-left', 'top-center', 'top-right', 'right-top', 'right-center', 'right-bottom', 'left-top', 'left-center', 'left-bottom']` | Направления раскрытия блока                               |
| mainOffset?               | `number`                                   | `0`                                                                                                                                                                              | Отступ попапа относительно основного направления          |
| motionless?               | `false \| true`                            | —                                                                                                                                                                                | Закрепляет положение попапа после открытия                |
| secondaryOffset?          | `number`                                   | `0`                                                                                                                                                                              | Отступ попапа относительно второстепенного направления    |
| tailOffset?               | `number`                                   | `0`                                                                                                                                                                              | Отступ хвостика от края попапа                            |
| target?                   | `"anchor"`                                 | —                                                                                                                                                                                | Позиционирование попапа относительно переданного элемента |
| viewportOffset?           | `number`                                   | `0`                                                                                                                                                                              | Отступ от края окна браузера                              |
| getPossibleDrawingParams? | `(drawingParams: DrawingParams[]) => void` | —                                                                                                                                                                                | Расчет параметров для отрисовки                           |

### Вид попапа

Чтобы изменить вид попапа, установите свойство `view` в значение `default`.

<Example
    height={100}
    component={PopupView}
    source={popupViewSources}
/>

### Классический дизайн

Чтобы задать тему попапа, установите свойство `theme` в одно из следующих значений: `"clear"`, `"normal"`.

<Disclaimer>
    <p>
    Важно! Модификаторы <b>view</b> и <b>theme</b> являются взаимоисключающими. Не используйте их вместе. <br />
    Модификатор <b>theme</b>  не кастомизируется с помощью дизайн-токенов.
    </p>
</Disclaimer>

<Example
    height={100}
    component={PopupTheme}
    source={popupThemeSources}
/>

Чтобы совсем убрать стилевое оформление, установите свойство `nonvisual` в значение `true`.

<Example
    height={100}
    component={PopupNonvisual}
    source={popupNonvisualSources}
/>

### Направление раскрытия попапа

Чтобы задать направление раскрытия попапа, используйте свойство `directions`.

<Example
    height={250}
    component={PopupDirection}
    source={popupDirectionSources}
/>

### Границы отображения попапа

Чтобы задать границы отображения попапа, используйте свойство `boundary`. Это может быть полезно, когда якорь находится внутри контейнера с `overflow: hidden` или вы хотите явно задать границы отображения. По-умолчанию, попап автоматически пытыется найти ближайшего родителя относительно якоря со свойством `overflow`.

<Example
    height={300}
    component={PopupBoundary}
    source={popupBoundarySources}
/>
